Propose bounded mutations in config space first.

Prioritize:
- loss family
- miner strategy
- proxy count
- temperature / margin
- augmentation recipe
- backbone freeze vs fine-tune

Do not propose arbitrary code rewrites unless the config search space is clearly exhausted.

