# Pinned runtime dependencies for the GHCR metric-search image.
#
# The Docker image starts from pytorch/pytorch:2.5.1-cuda12.4-cudnn9-runtime,
# which already provides torch==2.5.1+cu124 and torchvision==0.20.1+cu124.
# Keep torch/torchvision out of this file to avoid pip replacing the CUDA
# runtime with whatever the latest PyPI torch wheel is.
PyYAML==6.0.2
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.2
faiss-cpu==1.8.0.post1
timm==1.0.12
transformers==4.46.3
pytorch-metric-learning==2.8.1
tqdm==4.67.1
